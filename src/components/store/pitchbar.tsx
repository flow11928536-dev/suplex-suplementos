'use client'

// Este componente foi desativado porque o letreiro foi movido para o topo do Header.
// Retornar null garante que ele suma de todas as páginas (page.tsx) automaticamente.
export function Pitchbar() {
  return null
}